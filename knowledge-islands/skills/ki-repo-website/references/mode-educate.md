# EDUCATE

1. Decide whether the website is content-led or one interactive application.
2. Declare the core and exactly one implementation table.
3. Establish the root `ki:site:build`, `ki:site:dev`, and `ki:site:clean` scripts and a local `dist/` output.
4. Add a hosting adapter independently if publication requires one.
