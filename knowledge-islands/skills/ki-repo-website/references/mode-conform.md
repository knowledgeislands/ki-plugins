# CONFORM

Run `ki repo conform --skill ki-repo-website --dry-run --repo <repo>`. Review declaration and generated-output proposals. Choose or change the content/app implementation deliberately; conform does not infer architecture or run builds.
