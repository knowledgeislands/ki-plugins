# AUDIT

Run `ki repo audit --skill ki-repo-website-app --repo <repo>`, then the website core and any independently declared hosting adapter. Confirm all app evidence comes from the core-selected `site-root`. Build execution remains an explicit authorised verification step.
