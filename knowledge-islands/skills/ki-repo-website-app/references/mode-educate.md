# EDUCATE

Scaffold a TypeScript React Vite application at the chosen site root, expose the shared `ki:site:*` scripts, emit `dist/`, and declare `[skills.ki-repo-website]` plus `[skills.ki-repo-website-app]`. Add hosting separately.
