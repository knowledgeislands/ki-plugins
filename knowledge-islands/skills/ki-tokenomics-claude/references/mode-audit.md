# Mode AUDIT

Run `ki repo audit --skill ki-tokenomics-claude`. Read only the selected repository's physical source files, report direct structural observations, and explicitly mark effective-session facts and measurements unavailable rather than guessing.
