# Mode AUDIT

Run `ki repo audit --skill ki-tokenomics-claude`. Read only the selected repository and bounded user evidence, report estimated surfaces by layer, and explicitly mark unavailable measurements rather than guessing.
