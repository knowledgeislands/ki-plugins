# Mode READY — submit for approval

_On-demand procedure for streams' READY mode. The shared model — the zone-at-a-glance, the status lifecycle, the proposal anatomy, the bindings, Step 1, and the working rules + Enactment gate — lives in [`SKILL.md`](../SKILL.md) and is already loaded; this file is the procedure only._

Verify there are no open questions remaining and **every prerequisite in `dependencies` has reached `rolled-out`**; complete Outputs; then set `status: ready` and submit to the user. `ready` is a necessary condition for rollout, not authorisation to begin it.
