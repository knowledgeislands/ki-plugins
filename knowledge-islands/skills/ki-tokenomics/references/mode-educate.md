# Mode EDUCATE

Declare `[skills.ki-tokenomics]` in the selected repository only when it needs an override from the portable defaults. Choose a purpose-based `preferred_model_type`; select a runtime adapter separately.
