# Mode AUDIT

Run `ki repo audit --skill ki-tokenomics` to validate the selected repository's portable tokenomics policy. Use the configured runtime adapter to collect runtime evidence; selecting that adapter also selects this portable skill through its declared dependency. Report estimates, attribution, and owner routing separately; do not treat estimates as billing.
