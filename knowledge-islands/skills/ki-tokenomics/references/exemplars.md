# Tokenomics exemplars

```toml
[skills.ki-tokenomics]
preferred_model_type = "standard"

[skills.ki-tokenomics.budgets]
total = 30000
```

The declaration expresses portable policy. Runtime-specific model names, compression integration, and measured context surfaces are reported only by a selected runtime adapter.
