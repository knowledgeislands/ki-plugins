# Mode ROLLOUT — route delivery

_On-demand procedure for Streams ROLLOUT. The shared model lives in [`SKILL.md`](../SKILL.md) and is already loaded; this file is the procedure only._

For a ready roadmap item, load `ki-implement`. It owns delivery, review packet, and transition to `awaiting-review`; `ki-accept` owns closure. This container skill has no parallel rollout process.
