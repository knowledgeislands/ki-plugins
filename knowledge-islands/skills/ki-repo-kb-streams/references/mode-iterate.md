# Mode ITERATE — route work to its owner

_On-demand procedure for Streams ITERATE. The shared model lives in [`SKILL.md`](../SKILL.md) and is already loaded; this file is the procedure only._

For a roadmap item in `Streams/Roadmap/`, load `ki-plan` and the roadmap standard. For a template in `Streams/Housekeeping/`, load `ki-change-management-housekeeping`. This container skill does not maintain a generic proposal document, Focus index, or second lifecycle.
